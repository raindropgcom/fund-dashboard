# 2026-07-08

## 代码审查：AKshare 基金分析脚本集合
- 项目性质：基于 akshare 的基金净值/涨跌/交易看板个人脚本集（约 23 个 .py 混在根目录）。
- 主要问题：
  - 代码严重重复：`get_fund_metrics` 在 download_fund_history*.py / fund_analysis.py / import_today.py / update.py 中重复定义 6+ 次，签名与返回 dict（中英文字段名）各不相同。
  - 根目录混杂备份：`*副本.py`、`.old.py`、多版 `download_fund_history1~3` 应移出版本管理（用 git 代替）。
  - 硬编码绝对路径 `E:\AKshare\...` 遍布；`fund_update.bat` 调用 `fund_update.py`（不存在），实际文件是 `update.py` → 入口损坏。
  - 引用了不存在的 `E:\AKshare\trade\`（应为 `trades\`）：excel_auto_process.py / fund_excel_dashboard_v2.py / fund_dashboard_html.py。
  - 无 requirements.txt / README / .gitignore；几十个输出 xlsx 与代码混放；`~$*.xlsx` 锁文件残留。
  - import_today.py 用 f-string 拼 SQL（注入风险）；get_fund_name 每只基金都拉全量基金列表（性能差）；calculate_return 按行号而非真实日期算区间。
  - test.py 非真实测试，用 finplot 需 GUI，且硬编码标的/日期。
- 相对较好的部分：trades/archive_trades.py、timing_price/fund_dashboard_trading.py 结构清晰（配置区、日志、docstring、去重、MD5、交易时间/缓存/并发）。
- 结论：能跑的个人原型，但组织不合理，需拆包 + 抽公共模块 + 去硬编码 + 修入口。

## 重构执行（同日）
- 新建 `fundkit.py`：把 6+ 处重复的 get_fund_metrics 等收敛为唯一核心模块；相对路径(BASE_DIR)、基金名称一次性缓存、按真实日期算区间涨幅、补齐缺失的 get_fund_index_metrics、容忍 fund_list.txt 脏数据、统一中文字段返回。已 py_compile 通过且无 SyntaxWarning。
- 保留并修路径：`trades/archive_trades.py`、`timing_price/fund_dashboard_trading.py` 的 E:\AKshare 硬编码改为基于 __file__ 的相对路径。
- 隔离冗余：download_fund_history1~3/fund_analysis/import_today/update/各 dashboard 旧版·副本/fund_trader/trade_recommend/excel_auto_process/test 共 20 个文件移入 `_deprecated/`；删除 ~$ 锁文件。
- 修入口：`fund_update.bat` 改指向 `fundkit.py`（注：此步后来纠正，见下）。

## 修正 fund_update.bat + 看板 7-7 缺失 bug（同日 09:40）
- 问题：用户反映 `fund_code/fund_dashboard.html` 的"前7日历史"列只到 2026-07-06、缺 2026-07-07。
- 排查：F10 接口实测最新返回 2026-07-07（日涨幅0.88%），解析函数也正确 → 接口/解析无问题。
- 根因：`timing_price/fund_dashboard_trading.py` 的 `fetch_all_data` 中历史净值只在进程首次抓取并永久缓存（`need_hist` 仅首轮非空），常驻进程的历史列冻结在旧日期；且 `fund_update.bat` 被我错指到 `fundkit.py`（批量化析模块，非看板生成器）。
- 修复：
  1. `fund_update.bat` 改指 `timing_price\fund_dashboard_trading.py`（看板真正生成器）。
  2. 该脚本加 `HIST_REFRESH_MINUTES=30` 与全局 `last_hist_fetch`，历史净值改为"每 30 分钟强制全部重抓"，新交易日涨跌自动出现。已 py_compile 通过。
- 用户操作：关掉旧看板进程后重新跑 bat/脚本，HTML 即含 7-7。

## fund_update.bat 运行不了（同日 10:05）
- 现象：用户双击 bat 报"运行不了"。
- 根因：本机默认 `python`（含 Bash 沙箱里的）解析到托管 Python 3.13.12（C:\Users\Administrator\.workbuddy\binaries\python\versions\3.13.12\python.exe），它**未安装 requests/apscheduler/pandas/openpyxl** → `ModuleNotFoundError: No module named 'requests'`。而系统 Python（D:\Programs\Python\Python313\python.exe）依赖齐全，看板实际能跑（已实测，HTML 刷新出 2026-07-07）。
- 修复：`fund_update.bat` 改为"候选 Python 列表逐一检测能否 import 依赖，选第一个可用的"，优先 PATH 的 python，回退到已知系统 Python；缺依赖时给出清晰报错与 pip 安装提示。已停掉验证用的后台进程。
- 教训：该项目依赖未隔离；长期建议建 `requirements.txt` + 虚拟环境，避免依赖裸 `python` 解析到无依赖的解释器。
- 根目录现在只剩：fundkit.py、fund_update.bat、fund_list.txt、fund_config.txt、文字格式.txt + 子目录(trades/, timing_price/, fund_code/, fund_data/)。数据 xlsx 仍散在根目录，建议后续移 data/ 并加 .gitignore。

## 误删用户功能：交易标注版看板被我隔离（同日 10:10）
- 用户投诉："缺失了很多功能，比如前天买入卖出总金额被去掉了"。
- 根因：首轮清理时我把 `fund_dashboard_trade.py`（标题"基金实时估值 Dashboard（交易记录标注版+买入/卖出金额排序）"）误判为重复版本移入 `_deprecated/`。它才是用户**真正每天在用的全功能看板**：除实时估值+7日历史外，还从 `E:\AKshare\trades`（日期命名 xlsx，如 `2026-07-06.xlsx`）读取交易记录，显示「买入金额/卖出金额」列、买卖徽章、顶部总买入/卖出金额/交易基金数/交易日期。而保留的 `timing_price/fund_dashboard_trading.py` 是**纯估值版**（无交易功能），导致功能"消失"。
- 修复：
  1. 把 `fund_dashboard_trade.py` 移回根目录作唯一活动生成器；路径改相对(__file__)；补 `HIST_REFRESH_MINUTES=30`+`last_hist_fetch` 强制周期重抓历史（防 7-7 再次冻结）。
  2. 纯估值版改名 `fund_dashboard_valuation_only.py` 移入 `_deprecated/`。
  3. `fund_update.bat` 改指 `fund_dashboard_trade.py`。
- 验证：系统 Python 实测运行，HTML 含顶部「买入金额 2,400.00 / 卖出金额 0.00 / 交易基金数 / 交易日期 2026-07-06」、11 个买卖徽章、买入金额排序按钮。功能恢复。
- 教训：清理/去重前必须核对**功能差异**，不能因"看起来像另一个版本"就隔离；用户实际用的那个往往带特有功能。

## 云方案：手机出差看板（同日 10:47 起）
- 用户要出差只带手机看基金看板。讨论三条路：A 静态快照拷手机(¥0)、B 家里电脑远程(¥0~300/年)、C 上云。
- 用户经调研最终确定**方案 A（全自动，长期）**：CI=GitHub Actions(公开仓库免费) + 静态托管=腾讯云 COS(国内快, 免费额度50G存储+10G CDN/月) + Actions 自动上传 COS。Gitee Pages 普通版已下线/残缺；COS+Actions 为成熟方案。
- 已做（对本地无破坏）：
  1. `fund_dashboard_trade.py` 的 `main()` 加 `--once` 单次模式（CI 必需：生成一次即退出，否则 Action 卡死在 while True；本地默认行为不变）。
  2. 新建 `.github/workflows/update.yml`：cron 收紧到交易时段 `*/20 1-7 * * 1-5`(北京时间9:00-15:59工作日)、装依赖(requests pandas openpyxl lxml apscheduler coscmd)、`python fund_dashboard_trade.py --once`、coscmd 上传 `./fund_code` 到 COS。
- 待用户完成（我无法代做）：腾讯云注册→建公有读 COS 桶(如 ap-guangzhou)→获取 SecretId/SecretKey→GitHub 仓库 Secrets 填 COS_SECRET_ID/KEY/BUCKET/REGION→开启桶静态网站→Actions 手动跑一次测试。
- ⚠️ 关键未解风险：数据抓取在 GitHub **海外 runner**，东方财富可能限海外 IP → 空页面。COS 只托管不抓数，此风险 COS 救不了。**必须首次手动跑一次 Actions 检查 HTML 是否有真实数据**；若为空改 Gitee Go(国内 CI)。已向用户说明。
- 隐私：建议**私有仓库**(收紧cron后~660分/月<2000免费额度) + trades 提交到私有仓库(或 Secrets 加密)，避免公开暴露交易金额。
